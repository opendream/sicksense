# Example / documented defaults for grok-senpai workers.
# Copy to worker-config.toml or let install.sh create it.
# See AGENTS.md → "Worker models & thinking levels".

[claude]
# Fable + high; friendly aliases are defined in model-aliases.toml.
model = "fable"
effort = "high"

[codex]
model = "gpt-5.6-sol"
effort = "ultra"

[policy]
allow_override = true
enforce_floors = true
min_effort_claude = "high"
min_effort_codex = "high"
allow_unknown_model = false
require_launch_echo = true
